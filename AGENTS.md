# Codex agent instructions

- 中文回答。
- 禁止编译：不要执行任何编译或构建命令（例如 `m`、`mm`、`make`、`soong_ui`、`lunch`、`ninja` 等），除非用户明确要求或允许。
- 除非用户要求，`adb` 命令只需给出 Windows 适用命令。
- 界面元素使用文字显示即可，禁止使用 Emoji、小表情作为图标、按钮内容、状态标识或装饰。
- 禁止使用数字、字符串首个字符、单个文字或缩写等模拟小图标；缺少真实图片资源时直接隐藏对应图标或头像区域，不得生成文字占位头像或装饰性序号图标。
